import PlaceholderView from './PlaceholderView.jsx';

export default function SessionInterface() {
  return (
    <PlaceholderView title="Session Interface">
      Context assembly and creative-session persistence are stubbed for Phase 1.
    </PlaceholderView>
  );
}
