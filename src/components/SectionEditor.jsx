import PlaceholderView from './PlaceholderView.jsx';

export default function SectionEditor() {
  return (
    <PlaceholderView title="Section Editor">
      Editing, autosave, preview, versioning, and lock behavior are intentionally deferred.
    </PlaceholderView>
  );
}
