import PlaceholderView from './PlaceholderView.jsx';

export default function CharacterRelationshipMap() {
  return (
    <PlaceholderView title="Relationship Map">
      Map architecture is reserved for a later phase. D3 is intentionally absent from Phase 1.
    </PlaceholderView>
  );
}
