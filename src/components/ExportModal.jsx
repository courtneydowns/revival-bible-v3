import PlaceholderView from './PlaceholderView.jsx';

export default function ExportModal() {
  return (
    <PlaceholderView title="Export">
      Markdown, DOCX, and PDF IPC stubs exist. Export generation is deferred.
    </PlaceholderView>
  );
}
