import PlaceholderView from './PlaceholderView.jsx';

export default function OutbreakTimeline() {
  return (
    <PlaceholderView title="Outbreak Timeline">
      Timeline data and visualization are deferred. No D3 is loaded in Phase 1.
    </PlaceholderView>
  );
}
